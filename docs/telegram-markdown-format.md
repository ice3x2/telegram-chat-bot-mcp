# Telegram Markdown 메시지 포맷 가이드

## 목차
1. [개요](#개요)
2. [Telegram 메시지 포맷 비교](#telegram-메시지-포맷-비교)
3. [HTML 포맷 (권장)](#html-포맷-권장)
4. [MarkdownV2 포맷](#markdownv2-포맷)
5. [Google Chat Cards V2 → Telegram 매핑](#google-chat-cards-v2--telegram-매핑)
6. [이쁘게 표현하기: 베스트 프랙티스](#이쁘게-표현하기-베스트-프랙티스)
7. [이미지 처리 전략](#이미지-처리-전략)
8. [인라인 키보드 버튼](#인라인-키보드-버튼)
9. [실전 예시](#실전-예시)
10. [문제 해결](#문제-해결)

---

## 개요

### 목적
Markdown 텍스트를 Telegram 메시지로 **이쁘게 표현**하기 위한 포맷 가이드입니다. Google Chat의 Cards V2를 Telegram의 HTML/MarkdownV2 포맷으로 변환하는 방법과 시각적으로 매력적인 메시지를 작성하는 베스트 프랙티스를 제공합니다.

### Telegram 메시지 포맷 철학

Telegram은 텍스트 기반 포맷팅을 중심으로 설계되어 있습니다. HTML 태그와 이모지, 적절한 줄바꿈을 조합해 시각적 임팩트를 극대화하는 접근이 효과적입니다.

요약:
- **디자인 초점**: 텍스트 중심의 가독성과 간결성
- **시각적 요소**: 이모지, 구분선(유니코드), 인라인 키보드
- **복잡도**: 단순한 문자열 조합으로 높은 표현력 달성

핵심 인사이트: Telegram 메시지는 "텍스트의 예술"이며, 가독성 높은 구조와 이모지 조합으로 사용자의 주의를 끌 수 있습니다.

---

## Telegram 메시지 포맷 비교

Telegram은 3가지 메시지 포맷을 지원합니다:

| 포맷 | 설명 | 장점 | 단점 | 권장 사용 |
|------|------|------|------|----------|
| **Plain Text** | 서식 없음 | 항상 작동 | 밋밋함 | 폴백용 |
| **HTML** | HTML 태그 (`<b>`, `<i>`, `<a>`) | 직관적, 이스케이프 간단 | Telegram 전용 태그 일부 | ✅ **기본 권장** |
| **MarkdownV2** | Markdown 확장 문법 | 익숙한 문법 | 특수 문자 이스케이프 복잡 | 고급 사용자 |

### 권장 선택: **HTML**

**이유**:
1. **이스케이프가 간단**: `<`, `>`, `&`만 처리하면 됨
2. **가독성이 높음**: `<b>bold</b>`가 `*bold*`보다 명확
3. **중첩이 쉬움**: `<b><i>굵고 기울임</i></b>` 바로 가능
4. **Telegram 고유 기능**: `<tg-spoiler>`, `<u>` (밑줄) 사용 가능

---

## HTML 포맷 (권장)

### 지원 태그 전체 목록

| 태그 | 설명 | 예시 | 렌더링 |
|------|------|------|--------|
| `<b>text</b>` | 굵게 (Bold) | `<b>중요</b>` | **중요** |
| `<strong>text</strong>` | 굵게 (`<b>`와 동일) | `<strong>강조</strong>` | **강조** |
| `<i>text</i>` | 기울임 (Italic) | `<i>강조</i>` | *강조* |
| `<em>text</em>` | 기울임 (`<i>`와 동일) | `<em>강조</em>` | *강조* |
| `<u>text</u>` | 밑줄 (Underline) | `<u>밑줄</u>` | <u>밑줄</u> |
| `<s>text</s>` | 취소선 (Strikethrough) | `<s>삭제</s>` | ~~삭제~~ |
| `<strike>text</strike>` | 취소선 (`<s>`와 동일) | `<strike>삭제</strike>` | ~~삭제~~ |
| `<del>text</del>` | 취소선 (`<s>`와 동일) | `<del>삭제</del>` | ~~삭제~~ |
| `<code>text</code>` | 인라인 코드 | `<code>npm install</code>` | `npm install` |
| `<pre>text</pre>` | 코드 블록 | `<pre>const x = 1;</pre>` | ```const x = 1;``` |
| `<a href="url">text</a>` | 링크 | `<a href="https://google.com">Google</a>` | [Google](https://google.com) |
| `<tg-spoiler>text</tg-spoiler>` | 스포일러 (클릭 시 표시) | `<tg-spoiler>비밀</tg-spoiler>` | ████ (클릭 전) |
| `<tg-emoji emoji-id="...">🔥</tg-emoji>` | 커스텀 이모지 | `<tg-emoji emoji-id="5368324170671202286">👍</tg-emoji>` | 커스텀 이모지 |

### 중첩 태그

HTML은 **중첩**을 지원합니다:

```html
<b><i>굵고 기울임</i></b>
<b><u>굵고 밑줄</u></b>
<i><s>기울임 취소선</s></i>
<b><a href="https://example.com">굵은 링크</a></b>
```

**렌더링**:
- ***굵고 기울임***
- **<u>굵고 밑줄</u>**
- *~~기울임 취소선~~*
- **[굵은 링크](https://example.com)**

### 이스케이프 규칙

HTML 특수 문자만 이스케이프하면 됩니다:

| 문자 | 이스케이프 |
|------|-----------|
| `<` | `&lt;` |
| `>` | `&gt;` |
| `&` | `&amp;` |

**예시**:
```typescript
function escapeHTML(text: string): string {
  return text
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');
}

// 사용 예시
escapeHTML('if (x < 5 && y > 10)');
// 결과: 'if (x &lt; 5 &amp;&amp; y &gt; 10)'
```

### 코드 블록 (Syntax Highlighting 없음)

Telegram은 **기본적으로 syntax highlighting을 지원하지 않습니다**. 하지만 `<pre>` 태그로 코드 블록을 만들 수 있습니다:

```html
<pre>function hello() {
  console.log('Hello, Telegram!');
}</pre>
```

**렌더링**:
```
function hello() {
  console.log('Hello, Telegram!');
}
```

**팁**: 언어 정보는 주석으로 추가하세요:
```html
<pre>// JavaScript
function hello() {
  console.log('Hello');
}</pre>
```

---

## MarkdownV2 포맷

### 지원 문법

| 문법 | 설명 | 예시 | 렌더링 |
|------|------|------|--------|
| `*text*` | 굵게 (Bold) | `*bold*` | **bold** |
| `_text_` | 기울임 (Italic) | `_italic_` | *italic* |
| `__text__` | 밑줄 (Underline) | `__underline__` | <u>underline</u> |
| `~text~` | 취소선 (Strikethrough) | `~strike~` | ~~strike~~ |
| `` `text` `` | 인라인 코드 | `` `code` `` | `code` |
| ` ```text``` ` | 코드 블록 | ` ```block``` ` | ```block``` |
| `[text](url)` | 링크 | `[Google](https://google.com)` | [Google](https://google.com) |
| `||text||` | 스포일러 | `||spoiler||` | ████ (클릭 전) |

### 이스케이프 규칙 (복잡함!)

MarkdownV2는 **특수 문자를 모두 이스케이프**해야 합니다:

```
_ * [ ] ( ) ~ ` > # + - = | { } . !
```

**예시**:
```typescript
function escapeMarkdownV2(text: string): string {
  return text.replace(/[_*[\]()~`>#+=|{}.!-]/g, '\\$&');
}

// 사용 예시
escapeMarkdownV2('Hello (world)! Cost: $5.99');
// 결과: 'Hello \\(world\\)\\! Cost: \\$5\\.99'
```

**문제점**: 복잡하고 실수하기 쉽습니다. 따라서 **HTML 권장**.

---

## Cards V2 → Telegram 매핑 (레거시 카드형식 참조)

### 위젯 매핑표 (구조화된 카드 형식 → Telegram)

| Cards V2 위젯 (레거시) | Telegram 대체 방법 | 예시 |
|---------------------------|-------------------|------|
| **TextParagraph** | HTML `<b>`, `<i>` | `<b>제목</b>\n내용` |
| **Header** | HTML `<b>📌 제목</b>` | `<b>📌 빌드 완료</b>` |
| **Image** | HTML 링크 + 이모지 또는 `sendPhoto` API | `🖼️ <a href="url">이미지 보기</a>` |
| **Button** (URL) | InlineKeyboard 버튼 | `{ text: "열기", url: "..." }` |
| **Button** (Action) | InlineKeyboard 콜백 | `{ text: "확인", callback_data: "..." }` |
| **ButtonList** | InlineKeyboard 여러 줄 | `[[btn1, btn2], [btn3]]` |
| **Divider** | 구분선 이모지 | `➖➖➖➖➖` 또는 `━━━━━` |
| **Grid** | 테이블 유니코드 문자 | `│ A │ B │\n├───┼───┤` |
| **DecoratedText** | HTML + 이모지 | `✅ <b>성공</b>` |
| **SelectionInput** | ❌ 지원 안 됨 | (폴백: 버튼 목록) |
| **DateTimePicker** | ❌ 지원 안 됨 | (폴백: 텍스트 입력 안내) |

### Cards V2 → HTML 변환 예시

#### 기존 Cards V2 예시 (레거시)
```json
{
  "cardsV2": [
    {
      "cardId": "build-card",
      "card": {
        "header": {
          "title": "빌드 완료",
          "subtitle": "v2.1.0",
          "imageUrl": "https://example.com/icon.png"
        },
        "sections": [
          {
            "widgets": [
              {
                "textParagraph": {
                  "text": "프로젝트 빌드가 <b>성공</b>했습니다."
                }
              },
              {
                "divider": {}
              },
              {
                "decoratedText": {
                  "topLabel": "버그 수정",
                  "text": "3개"
                }
              },
              {
                "buttonList": {
                  "buttons": [
                    {
                      "text": "로그 보기",
                      "onClick": {
                        "openLink": {
                          "url": "https://logs.example.com"
                        }
                      }
                    }
                  ]
                }
              }
            ]
          }
        ]
      }
    }
  ]
}
```

#### 변환된 Telegram HTML
```html
<b>📌 빌드 완료</b>
<i>v2.1.0</i>

프로젝트 빌드가 <b>성공</b>했습니다.

━━━━━━━━━━━━━━━━━━━

🐛 <b>버그 수정</b>: 3개
```

**+ InlineKeyboard 버튼**:
```json
{
  "inline_keyboard": [
    [
      { "text": "📊 로그 보기", "url": "https://logs.example.com" }
    ]
  ]
}
```

---

## 이쁘게 표현하기: 베스트 프랙티스

### 1. 이모지 활용 (필수!)

Telegram에서 **이모지는 시각적 구분의 핵심**입니다.

| 용도 | 이모지 | 예시 |
|------|--------|------|
| **제목** | 📌 🎯 🔔 | `<b>📌 알림</b>` |
| **성공** | ✅ 🎉 🚀 | `✅ <b>배포 성공</b>` |
| **실패/오류** | ❌ ⚠️ 🚨 | `❌ <b>빌드 실패</b>` |
| **정보** | ℹ️ 💡 📝 | `ℹ️ <i>참고 사항</i>` |
| **작업 중** | ⏳ 🔄 🔧 | `⏳ <b>배포 중...</b>` |
| **버그** | 🐛 🔴 | `🐛 버그 수정: 3개` |
| **성능** | ⚡ 📈 | `⚡ 속도 개선: 20%` |
| **보안** | 🔒 🛡️ | `🔒 보안 업데이트` |
| **이미지** | 🖼️ 📷 🎨 | `🖼️ <a href="url">스크린샷</a>` |
| **파일** | 📄 📦 | `📦 패키지 업로드 완료` |
| **링크** | 🔗 🌐 | `🔗 <a href="url">문서 보기</a>` |
| **시간** | ⏰ 📅 | `⏰ 배포 시간: 14:30` |
| **사용자** | 👤 👥 | `👤 배포자: Alice` |
| **코드** | 💻 ⌨️ | `💻 <code>npm install</code>` |

**나쁜 예시** (밋밋함):
```
빌드 완료
프로젝트가 성공적으로 빌드되었습니다.
```

**좋은 예시** (임팩트!):
```
✅ 빌드 완료

프로젝트가 <b>성공적으로</b> 빌드되었습니다. 🎉
```

### 2. 구조화 (제목 + 내용 + 액션)

메시지를 3단계로 구조화하세요:

```
1️⃣ 제목 (굵게 + 이모지)
2️⃣ 내용 (핵심 정보)
3️⃣ 액션 (버튼 또는 링크)
```

**예시**:
```html
<b>🚀 배포 완료</b>

프로젝트 <code>my-app</code>이 <b>프로덕션</b> 환경에 배포되었습니다.

⏰ 시간: 2025-11-03 14:30
👤 배포자: Alice
🔗 <a href="https://app.example.com">앱 열기</a>
```

### 3. 시각적 구분선

Google Chat의 `divider` 위젯 대신 유니코드 문자 사용:

| 구분선 스타일 | 예시 |
|--------------|------|
| **가는 선** | `➖➖➖➖➖` |
| **굵은 선** | `━━━━━━━━━━━━━━━━━━━` |
| **점선** | `┄┄┄┄┄┄┄┄┄┄` |
| **이중선** | `═════════════` |
| **별** | `✦ ✦ ✦ ✦ ✦` |

**사용 예시**:
```html
<b>📌 빌드 결과</b>

✅ 성공: 3개
❌ 실패: 1개

━━━━━━━━━━━━━━━━━━━

ℹ️ <i>자세한 내용은 로그를 확인하세요.</i>
```

### 4. 리스트 스타일

| 스타일 | 예시 |
|--------|------|
| **기본 불릿** | `• 항목 1\n• 항목 2` |
| **이모지 불릿** | `✅ 완료\n⏳ 진행 중\n❌ 실패` |
| **번호** | `1. 첫째\n2. 둘째` |
| **화살표** | `▸ 항목 1\n▸ 항목 2` |
| **체크박스** | `☑️ 완료\n☐ 미완료` |

**예시**:
```html
<b>📋 작업 상태</b>

✅ 빌드 완료
✅ 테스트 통과
⏳ 배포 진행 중
☐ 알림 전송 대기
```

### 5. 정보 강조 (DecoratedText 대체)

Google Chat의 `decoratedText`를 HTML + 이모지로 변환:

```html
🐛 <b>버그 수정</b>: 3개
⚡ <b>성능 개선</b>: 20%
📦 <b>패키지 크기</b>: 1.2 MB
👥 <b>기여자</b>: 5명
```

### 6. 테이블 스타일 (Grid 대체)

유니코드 박스 문자로 테이블 생성:

```
┌─────────┬─────────┐
│  항목   │  값     │
├─────────┼─────────┤
│ 빌드    │ 성공    │
│ 테스트  │ 통과    │
│ 배포    │ 완료    │
└─────────┴─────────┘
```

**간단한 버전**:
```
항목     | 값
─────────┼─────────
빌드     | 성공
테스트   | 통과
배포     | 완료
```

**가장 간단한 버전** (권장):
```html
<b>📊 결과</b>

빌드: ✅ 성공
테스트: ✅ 통과
배포: ✅ 완료
```

### 7. 색상 대체 (이모지 활용)

Telegram은 텍스트 색상을 지원하지 않습니다. 대신 **이모지로 색상 느낌**을 표현:

| 색상 의도 | 이모지 |
|----------|--------|
| **빨강** (오류) | 🔴 ❌ 🚨 |
| **녹색** (성공) | 🟢 ✅ 🎉 |
| **노랑** (경고) | 🟡 ⚠️ 💡 |
| **파랑** (정보) | 🔵 ℹ️ 📘 |
| **보라** (중요) | 🟣 ⭐ |

**예시**:
```html
<b>🔔 시스템 알림</b>

🟢 <b>API 서버</b>: 정상
🟡 <b>데이터베이스</b>: 경고 (응답 느림)
🔴 <b>캐시 서버</b>: 오류 (연결 실패)
```

---

## 이미지 처리 전략

Telegram은 2가지 이미지 전송 방식을 지원합니다:

### 방법 1: 링크 (HTML)

**장점**: 간단, 메시지에 포함
**단점**: 이미지가 메시지에 미리 보이지 않음 (링크만 표시)

```html
🖼️ <a href="https://example.com/image.jpg">스크린샷 보기</a>
```

### 방법 2: sendPhoto API

**장점**: 이미지가 메시지에 바로 표시됨
**단점**: 별도 API 호출 필요

```typescript
await axios.post(`https://api.telegram.org/bot${token}/sendPhoto`, {
  chat_id: chatId,
  photo: 'https://example.com/image.jpg',
  caption: '<b>배포 스크린샷</b>',
  parse_mode: 'HTML'
});
```

### 권장 전략: 하이브리드

1. **중요한 이미지** (스크린샷, 다이어그램) → `sendPhoto` API
2. **보조 이미지** (아이콘, 로고) → HTML 링크

**구현 예시** (`markdownToTelegram.ts`):
```typescript
function processInlineMarkdown(text: string): string {
  // ![alt](url) → 🖼️ <a href="url">alt</a>
  text = text.replace(/!\[([^\]]*)\]\(([^)]+)\)/g, (match, alt, url) => {
    // 중요 이미지 감지 (예: 'screenshot', 'diagram' 키워드)
    if (alt.toLowerCase().includes('screenshot') || alt.toLowerCase().includes('diagram')) {
      // TODO: sendPhoto API 호출 큐에 추가
      return `🖼️ <a href="${url}">${alt}</a> (별도 전송됨)`;
    } else {
      // 일반 이미지는 링크로
      return `🖼️ <a href="${url}">${alt}</a>`;
    }
  });

  return text;
}
```

### 이미지 검증 재사용

기존 `imageValidator.ts`를 그대로 사용:

```typescript
import { validateImageUrl } from '../utils/imageValidator.js';

const validation = await validateImageUrl(imageUrl);
if (!validation.valid) {
  logger.warn('Invalid image URL', { url: imageUrl, error: validation.error });
  // 폴백: 링크만 표시
  return `🖼️ <a href="${imageUrl}">이미지 보기</a>`;
}
```

---

## 인라인 키보드 버튼

### 기본 사용법

**JSON 구조**:
```json
{
  "inline_keyboard": [
    [
      { "text": "버튼 1", "url": "https://example.com" }
    ],
    [
      { "text": "버튼 2", "callback_data": "action_2" },
      { "text": "버튼 3", "callback_data": "action_3" }
    ]
  ]
}
```

**렌더링** (Telegram 앱에서):
```
┌──────────────┐
│   버튼 1     │  (클릭 시 URL 열기)
├──────┬───────┤
│버튼 2│버튼 3 │  (클릭 시 콜백 전송)
└──────┴───────┘
```

### 버튼 타입

| 타입 | 속성 | 설명 | 예시 |
|------|------|------|------|
| **URL 버튼** | `url` | 외부 링크 열기 | `{ "text": "열기", "url": "https://..." }` |
| **Callback 버튼** | `callback_data` | 봇에 데이터 전송 (양방향) | `{ "text": "확인", "callback_data": "confirm" }` |
| **Switch Inline** | `switch_inline_query` | 인라인 모드 활성화 | `{ "text": "공유", "switch_inline_query": "text" }` |
| **게임** | `callback_game` | 게임 시작 | `{ "text": "플레이", "callback_game": {} }` |
| **결제** | `pay` | 결제 시작 | `{ "text": "구매", "pay": true }` |

### Google Chat ButtonList → InlineKeyboard 변환

**기존 Google Chat Cards V2**:
```json
{
  "buttonList": {
    "buttons": [
      {
        "text": "로그 보기",
        "onClick": {
          "openLink": {
            "url": "https://logs.example.com"
          }
        }
      },
      {
        "text": "재시도",
        "onClick": {
          "action": {
            "function": "retry"
          }
        }
      }
    ]
  }
}
```

**변환된 Telegram InlineKeyboard**:
```json
{
  "inline_keyboard": [
    [
      { "text": "📊 로그 보기", "url": "https://logs.example.com" }
    ],
    [
      { "text": "🔄 재시도", "callback_data": "retry" }
    ]
  ]
}
```

### 버튼 스타일 가이드

**이모지 추가로 버튼을 더 이쁘게**:

| 버튼 용도 | 이모지 | 예시 |
|----------|--------|------|
| **문서/로그** | 📊 📄 📝 | `{ "text": "📊 로그 보기", ... }` |
| **열기/실행** | 🚀 ▶️ 🔗 | `{ "text": "🚀 앱 열기", ... }` |
| **설정** | ⚙️ 🔧 | `{ "text": "⚙️ 설정", ... }` |
| **확인/승인** | ✅ ☑️ | `{ "text": "✅ 확인", ... }` |
| **취소/거부** | ❌ 🚫 | `{ "text": "❌ 취소", ... }` |
| **재시도** | 🔄 ↻ | `{ "text": "🔄 재시도", ... }` |
| **다운로드** | ⬇️ 💾 | `{ "text": "⬇️ 다운로드", ... }` |
| **업로드** | ⬆️ 📤 | `{ "text": "⬆️ 업로드", ... }` |
| **공유** | 🔗 📤 | `{ "text": "🔗 공유", ... }` |
| **도움말** | ❓ 💡 | `{ "text": "❓ 도움말", ... }` |

---

## 실전 예시

### 예시 1: 빌드 완료 알림

**Google Chat Cards V2 스타일**:
```json
{
  "header": { "title": "빌드 완료", "subtitle": "v2.1.0" },
  "sections": [
    {
      "widgets": [
        { "textParagraph": { "text": "프로젝트 빌드가 성공했습니다." } },
        { "decoratedText": { "topLabel": "커밋", "text": "abc1234" } },
        { "decoratedText": { "topLabel": "브랜치", "text": "main" } },
        { "buttonList": {
          "buttons": [
            { "text": "로그 보기", "onClick": { "openLink": { "url": "..." } } }
          ]
        }}
      ]
    }
  ]
}
```

**Telegram 변환 (HTML + InlineKeyboard)**:

**HTML**:
```html
<b>🚀 빌드 완료</b>
<i>v2.1.0</i>

프로젝트 빌드가 <b>성공</b>했습니다. 🎉

━━━━━━━━━━━━━━━━━━━

📝 <b>커밋</b>: <code>abc1234</code>
🌿 <b>브랜치</b>: <code>main</code>
⏰ <b>시간</b>: 14:30
```

**InlineKeyboard**:
```json
{
  "inline_keyboard": [
    [
      { "text": "📊 로그 보기", "url": "https://logs.example.com" }
    ]
  ]
}
```

**전체 API 요청**:
```json
{
  "chat_id": 123456789,
  "text": "<b>🚀 빌드 완료</b>\n<i>v2.1.0</i>\n\n프로젝트 빌드가 <b>성공</b>했습니다. 🎉\n\n━━━━━━━━━━━━━━━━━━━\n\n📝 <b>커밋</b>: <code>abc1234</code>\n🌿 <b>브랜치</b>: <code>main</code>\n⏰ <b>시간</b>: 14:30",
  "parse_mode": "HTML",
  "reply_markup": {
    "inline_keyboard": [
      [
        { "text": "📊 로그 보기", "url": "https://logs.example.com" }
      ]
    ]
  }
}
```

### 예시 2: 오류 알림

**HTML**:
```html
<b>❌ 배포 실패</b>

프로덕션 배포 중 <b>오류</b>가 발생했습니다.

━━━━━━━━━━━━━━━━━━━

🐛 <b>오류 메시지</b>:
<code>Error: Connection timeout</code>

📝 <b>커밋</b>: <code>def5678</code>
🌿 <b>브랜치</b>: <code>release</code>
⏰ <b>시간</b>: 15:45

━━━━━━━━━━━━━━━━━━━

ℹ️ <i>DevOps 팀에 자동으로 알림이 전송되었습니다.</i>
```

**InlineKeyboard**:
```json
{
  "inline_keyboard": [
    [
      { "text": "📊 로그 보기", "url": "https://logs.example.com" },
      { "text": "🔔 알림 설정", "url": "https://settings.example.com" }
    ],
    [
      { "text": "🔄 재배포", "callback_data": "retry_deploy" }
    ]
  ]
}
```

### 예시 3: 테스트 결과

**HTML**:
```html
<b>🧪 테스트 완료</b>

총 <b>120개</b> 테스트가 실행되었습니다.

━━━━━━━━━━━━━━━━━━━

<b>📊 결과</b>

✅ 통과: 115개 (95.8%)
❌ 실패: 5개 (4.2%)
⏭️ 건너뜀: 0개

━━━━━━━━━━━━━━━━━━━

<b>⏱️ 실행 시간</b>
전체: 2분 34초
평균: 1.3초/테스트

━━━━━━━━━━━━━━━━━━━

<b>❌ 실패한 테스트</b>

1. <code>test_user_authentication</code>
2. <code>test_payment_validation</code>
3. <code>test_email_sending</code>
4. <code>test_database_connection</code>
5. <code>test_api_rate_limit</code>
```

**InlineKeyboard**:
```json
{
  "inline_keyboard": [
    [
      { "text": "📄 전체 리포트", "url": "https://report.example.com" }
    ],
    [
      { "text": "🐛 이슈 생성", "url": "https://github.com/repo/issues/new" },
      { "text": "🔄 재실행", "callback_data": "rerun_tests" }
    ]
  ]
}
```

### 예시 4: Markdown 입력 → Telegram HTML

**입력 (Markdown)**:
```markdown
# 배포 완료 🚀

프로젝트 **my-app**이 프로덕션 환경에 배포되었습니다.

## 변경 사항
- 버그 수정 3개
- 성능 개선 20%
- 새로운 기능 추가

코드 예시:
```bash
npm run deploy --env=production
```

[문서 보기](https://docs.example.com)

![스크린샷](https://example.com/screenshot.jpg)
```

**출력 (Telegram HTML)**:
```html
<b>📌 배포 완료 🚀</b>

프로젝트 <b>my-app</b>이 프로덕션 환경에 배포되었습니다.

<b>변경 사항</b>

• 버그 수정 3개
• 성능 개선 20%
• 새로운 기능 추가

코드 예시:
<pre>npm run deploy --env=production</pre>

🔗 <a href="https://docs.example.com">문서 보기</a>

🖼️ <a href="https://example.com/screenshot.jpg">스크린샷</a>
```

---

## 문제 해결

### Q: HTML 파싱 오류 "Bad Request: can't parse entities"

**원인**: HTML 특수 문자가 이스케이프되지 않음.

**해결책**:
```typescript
function escapeHTML(text: string): string {
  return text
    .replace(/&/g, '&amp;')   // & 먼저!
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');
}

// 예시
const userInput = 'if (x < 5 && y > 10)';
const safe = escapeHTML(userInput);
// 결과: 'if (x &lt; 5 &amp;&amp; y &gt; 10)'
```

### Q: 메시지가 너무 길어서 전송 실패

**원인**: Telegram 메시지 길이 제한은 **4096자**.

**해결책 1**: 메시지 분할
```typescript
function splitMessage(text: string, maxLength: number = 4096): string[] {
  const parts: string[] = [];
  let currentPart = '';

  const lines = text.split('\n');

  for (const line of lines) {
    if ((currentPart + line + '\n').length > maxLength) {
      parts.push(currentPart.trim());
      currentPart = line + '\n';
    } else {
      currentPart += line + '\n';
    }
  }

  if (currentPart.trim()) {
    parts.push(currentPart.trim());
  }

  return parts;
}

// 사용 예시
const messageParts = splitMessage(longHTML);
for (const part of messageParts) {
  await sendTelegramText({ text: part, chatId }, botToken, defaultChatId);
}
```

**해결책 2**: 파일로 전송 (매우 긴 메시지)
```typescript
await axios.post(`https://api.telegram.org/bot${token}/sendDocument`, {
  chat_id: chatId,
  document: 'attach://file',
  caption: '로그 파일'
}, {
  headers: { 'Content-Type': 'multipart/form-data' },
  data: {
    file: Buffer.from(longText, 'utf-8')
  }
});
```

### Q: 이모지가 깨져 보임

**원인**: UTF-8 인코딩 문제.

**해결책**: axios 요청에 UTF-8 명시
```typescript
const response = await axios.post(url, payload, {
  headers: { 'Content-Type': 'application/json; charset=utf-8' },
  timeout: 5000
});
```

### Q: InlineKeyboard 버튼이 표시되지 않음

**원인**: `reply_markup` JSON 구조 오류.

**해결책**: 타입 검증
```typescript
interface InlineKeyboardButton {
  text: string;
  url?: string;
  callback_data?: string;
}

interface InlineKeyboardMarkup {
  inline_keyboard: InlineKeyboardButton[][];
}

function validateInlineKeyboard(markup: InlineKeyboardMarkup): boolean {
  if (!markup.inline_keyboard || !Array.isArray(markup.inline_keyboard)) {
    return false;
  }

  for (const row of markup.inline_keyboard) {
    if (!Array.isArray(row)) return false;

    for (const button of row) {
      if (!button.text) return false;
      if (!button.url && !button.callback_data) return false;
    }
  }

  return true;
}
```

### Q: 코드 블록에서 들여쓰기가 깨짐

**원인**: HTML `<pre>` 태그는 공백을 유지하지만, 일부 클라이언트에서 렌더링 차이.

**해결책**: 백틱 3개로 감싸기 (MarkdownV2)
```
```language
code with indentation
```
```

또는 `<pre><code>` 조합:
```html
<pre><code class="language-javascript">
function hello() {
  console.log('indented');
}
</code></pre>
```

**참고**: Telegram은 `class` 속성을 무시하므로 syntax highlighting은 없습니다.

---

## 참고 자료

- [Telegram Bot API - Formatting Options](https://core.telegram.org/bots/api#formatting-options)
- [Telegram Bot API - InlineKeyboardMarkup](https://core.telegram.org/bots/api#inlinekeyboardmarkup)
- [Telegram Bot API - sendPhoto](https://core.telegram.org/bots/api#sendphoto)
- [Unicode Box Drawing Characters](https://en.wikipedia.org/wiki/Box-drawing_character)
- [Marked.js Documentation](https://marked.js.org/)

---

## 체크리스트

### Markdown → HTML 변환 구현
- [ ] `markdownToTelegram.ts` 생성
- [ ] 헤더 변환 (H1 → `<b>📌 H1</b>`)
- [ ] 인라인 포맷 변환 (`**bold**` → `<b>bold</b>`)
- [ ] 코드 블록 변환 (` ```code``` ` → `<pre>code</pre>`)
- [ ] 링크 변환 (`[text](url)` → `<a href="url">text</a>`)
- [ ] 이미지 처리 (`![alt](url)` → `🖼️ <a href="url">alt</a>`)
- [ ] 리스트 변환 (`- item` → `• item`)
- [ ] 이스케이프 함수 (`escapeHTML`)

### 이쁘게 만들기
- [ ] 이모지 매핑표 적용 (제목, 성공, 실패 등)
- [ ] 구분선 추가 (━━━━━━━━━━━━━━━━━━━)
- [ ] 정보 강조 (`🐛 <b>버그</b>: 3개`)
- [ ] 3단계 구조 (제목 + 내용 + 액션)

### 테스트
- [ ] 단위 테스트 (markdownToTelegram)
- [ ] 통합 테스트 (실제 Telegram 전송)
- [ ] 긴 메시지 분할 테스트
- [ ] 이모지 렌더링 테스트

---

**문서 버전**: 1.0  
**최종 업데이트**: 2025-11-03  
**작성자**: GitHub Copilot
